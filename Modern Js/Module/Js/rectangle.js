let rectangle = "afwefdwefwef";

function area(w , b) {
    return "rectangle area is " + w * b;
}

function volume(w , b , h) {
    return "rectangle volume is " + (w * b * h);
}

export {rectangle , area , volume}